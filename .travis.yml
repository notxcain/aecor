language: scala
scala:
  - 2.12.4

jdk:
  - oraclejdk8

cache:
  directories:
    - .hydra
    - $HOME/.m2
    - $HOME/.ivy2/cache
    - $HOME/.sbt
    - $HOME/.coursier
    - $HOME/.cache

script:
  - sbt ++$TRAVIS_SCALA_VERSION! clean validate